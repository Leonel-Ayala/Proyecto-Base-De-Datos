-- zi
CREATE TABLE LAROATLB_REGION_CLIENTE (
  ID_REGION NUMBER,
  NOMBRE_REGION VARCHAR2(50),
  CONSTRAINT PK_NIGGER_REGION_CLIENTE PRIMARY KEY(ID_REGION)
);

CREATE TABLE LAROATLB_CLIENTE(
  ID_CLIENTE NUMBER,
  NOMBRE VARCHAR2(80),
  APELLIDO1 VARCHAR2(30),
  APELLIDO2 VARCHAR2(30),
  TELEFONO NUMBER,
  EMAIL VARCHAR2(80),
  ID_REGION NUMBER,
  CONSTRAINT PK_LAROATLB_CLIENTE PRIMARY KEY(ID_CLIENTE),
  CONSTRAINT FK_LAROATLB_REGION FOREIGN KEY(ID_REGION) REFERENCES LAROATLB_REGION_CLIENTE(ID_REGION)
);

CREATE TABLE 
